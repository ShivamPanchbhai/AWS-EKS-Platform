apiVersion: v1
kind: ServiceAccount
metadata:
  # Name must match the serviceAccountName referenced in deployment.yaml.
  name: {{ .Values.serviceAccount.name }}
  namespace: {{ .Values.namespace }}
  labels:
    app: {{ .Release.Name }}